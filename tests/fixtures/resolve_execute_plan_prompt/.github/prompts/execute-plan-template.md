---
applyTo: "**"
---
# Test Template - `${name}`

You are the <agent_type> agent working on ${name}.

## Context

The feature folder is: ${folderpath}
The target file is: ${file}
The spec is at: ${spec}
The research is at: ${research}
The user story is at: ${user-story}
