# 2025-12-18-docs-v3-upgrade — Spec

- Issue: #54
- Owner: name
- Last Updated: 2025-12-19

## Overview

The current documentation workflow relies on ad hoc updates to instructions, prompts, and agent guidelines. Contributors report difficulty locating high-quality exemplars, and autogenerated pull request summaries lack the specificity reviewers need to assess risk. This results in inconsistent onboarding for new agents, duplicated documentation patterns, and review cycles slowed by vague summaries that miss change intent, breaking risks, or verification steps.


## Behavior

Introduce a documented, repeatable documentation enhancement program that curates best-in-class samples and enforces richer automation outputs.

- Establish a reference library of prompts, instructions, and agent behaviors that reflect current policy-compliant exemplars.
- Define a documentation governance workflow that flags outdated guidance and schedules recurring audits.
- Replace the current generic pull request summary generator with a template-aware engine that captures findings, risks, verification, and rollout notes tailored to the change category.
- Provide contributor-facing guidance (checklists, quick references, short videos) illustrating how to adapt and extend the curated assets.
- Instrument feedback loops (surveys, review comments taxonomy) so gaps in documentation or summary quality trigger updates.
- Strengthen PR context collection: separate merged PRs vs issues, validate issues via GitHub metadata, and embed verified autoclose targets.
- Embed “why” excerpts from active feature docs (spec/plan) plus PR-intent scaffolding directly into pr_context outputs to keep summaries grounded.
- Align PR author prompt/agent rules with the verified context: explicit GitHub Auto-close section, related references separated, and guardrails against hallucinated issue numbers.


## Inputs / Outputs

- Inputs
	- Changed files list, commit history for the PR range
	- GitHub CLI availability and auth state (for issue/PR validation and closingIssuesReferences)
	- Active feature folder paths (e.g., docs/features/active/<feature>/)
	- Feature doc content: spec.md (Context, Root Cause/Problem, Proposed Fix, Acceptance Criteria), plan.md (completed tasks, verification notes)
	- Optional author-provided PR intent fields (primary outcome, impact, risks, asserted autoclose issues)
- Outputs
	- pr_context.txt with: merged PRs, verified issues to autoclose, verified/non-closing references, PR intent scaffold, feature-doc excerpts, change stats, git status
	- Structured PR body sections (Findings/Changes, Why, Verification, Risks, GitHub Auto-close, Related) generated by the PR author workflow
	- Telemetry/feedback signals on documentation quality and template usage (if enabled)

## API / CLI Surface

- Collector: scripts/dev_tools/collect_pr_context.py
	- Flags/inputs: target base/head refs (from git), optional overrides for feature folder discovery, optional disable-gh-validation switch (if offline)
	- Behavior: produce pr_context.txt including PR intent template, validated autoclose list (when gh is available), feature-doc excerpts, and separated issue/PR references
- PR author prompt + agent: generate-pr.prompt.md with pr-author.agent.md
	- Behavior: consume pr_context.txt (and only enumerated additional context files) to render PR body with `## GitHub Auto-close` using verified issues only, `## Related` for non-closing refs, and richer “Why” from embedded excerpts
- Commit messaging remains via commit-steward.agent.md + generate-commit-message.prompt.md (reference only; unchanged but used as quality benchmark)

## Data & State

- Artifacts: pr_context.txt stored under artifacts/ with validated issue/PR buckets, PR intent fields, and feature-doc excerpts
- Feature-doc parsing: deterministic extraction of key sections (Context/Root Cause/Proposed Fix/Acceptance Criteria; completed plan tasks) without mutating source docs
- Telemetry: optional collection of template usage, PR summary completeness, reviewer satisfaction (survey/tagging) for governance reporting
- No persistent application state; relies on git metadata and current workspace files at generation time

## Constraints & Risks

- Maintaining alignment with existing repository policies is mandatory; curated examples must not contradict current guardrails.
- Expanding automation may require CI or GitHub App updates, which introduces coordination with DevOps maintainers.
- Overly rigid templates could discourage nuanced summaries if not designed with flexibility for edge cases.
- Collecting reviewer feedback must respect privacy expectations and avoid adding excessive toil.
- GitHub validation may be unavailable (offline/unauthenticated); workflows must fail safe by avoiding autoclose claims when unverified.
- Misclassification of PR numbers as issues is a known risk; separation and validation are mandatory to avoid hallucinated closures.
- Feature-doc extraction must remain deterministic and avoid leaking unrelated content; scope only specified sections.


## Definition of Done

- [ ] Behavior matches acceptance criteria
- [ ] Tests updated/added
- [ ] Docs updated (README, docs/features/active/... links)
- [ ] Telemetry/logging (if applicable)
- [ ] pr_context.txt includes validated issue/PR separation, feature-doc excerpts, and PR intent scaffold
- [ ] PR author outputs use deterministic GitHub Auto-close and Related sections aligned to verified references
- [ ] Governance owners receive adoption/quality signals (template usage, PR summary completeness, reviewer satisfaction)

## Seeded Test Conditions (from potential)
- [ ] Validate that autogenerated PR summaries satisfy template requirements across multiple change scenarios (feature, bugfix, documentation-only).
- [ ] Unit tests or integration tests cover new summary-generation logic, including fallback behavior when metadata is missing.
- [ ] Pilot review sessions confirm that curated prompts and instructions improve consistency without causing regressions in policy adherence.
- [ ] Measure reviewer satisfaction and turnaround time before and after rollout to confirm improved signal quality.

---
<small>Generated with GitHub Copilot as directed by {USER_NAME_PLACEHOLDER}</small>

